# ── Ollama ──────────────────────────────────────
ANALYZER_MODEL=cambiar_esto
EMBEDDING_MODEL=cambiar_esto
OLLAMA_API_URL=cambiar_esto
OLLAMA_BASE_URL=cambiar_esto
LLM_TEMPERATURE=cambiar_esto
LLM_NUM_CTX=cambiar_esto
LLM_NUM_PREDICT=cambiar_esto

# ── ChromaDB ────────────────────────────────────
CHROMA_HOST=cambiar_esto
CHROMA_PORT=cambiar_esto
CHROMA_NVD_COLLECTION=cambiar_esto
CHROMA_EXPLOIT_COLLECTION=cambiar_esto
CHROMA_PAGE_SIZE=cambiar_esto
CHROMA_QUERY_RESULTS=cambiar_esto

# ── NVD API ─────────────────────────────────────
NVD_API_BASE_URL=cambiar_esto
NVD_DAYS_BACK=cambiar_esto
NVD_PAGE_SIZE=cambiar_esto
NVD_API_TIMEOUT=cambiar_esto
NVD_API_DELAY=cambiar_esto
NVD_LAST_UPDATE_FILE=cambiar_esto
NVD_UPDATE_INTERVAL_DAYS=cambiar_esto

# ── Embeddings ──────────────────────────────────
EMBED_BATCH_TIMEOUT=cambiar_esto
EMBED_MAX_RETRIES=cambiar_esto
EMBED_SINGLE_TIMEOUT=cambiar_esto

# ── Análisis ────────────────────────────────────
ANALYSIS_CHUNK_SIZE=cambiar_esto
ANALYSIS_QUERY_LENGTH=cambiar_esto
REPORT_OUTPUT_DIR=cambiar_esto

# ── MongoDB ─────────────────────────────────────
MONGO_URI=cambiar_esto
MONGO_DATABASE_NAME=cambiar_esto
MONGO_TIMEOUT_MS=cambiar_esto

# ── ExploitDB ───────────────────────────────────
EXPLOITDB_REPO_URL=cambiar_esto
EXPLOITDB_LOCAL_DIR=cambiar_esto
EXPLOIT_BATCH_SIZE=cambiar_esto
EXPLOIT_MAX_TEXT_LENGTH=cambiar_esto

# ── NVD batch ───────────────────────────────────
NVD_BATCH_SIZE=cambiar_esto

# ── Celery ──────────────────────────────────────
CELERY_BROKER_URL=cambiar_esto
CELERY_RESULT_BACKEND=cambiar_esto
